language: java
jdk:
  - oraclejdk8
addons:
  postgresql: "9.3"
install:
  - mvn clean
before_script:
  - psql -c 'create role miniprojekti with createdb login;' -U postgres
  - psql -c 'create database miniprojekti;' -U miniprojekti
  - mvn flyway:migrate
script:
  - mvn integration-test
notifications:
  irc:
    channels:
      - "openirc.snt.utwente.nl#superryhmä"