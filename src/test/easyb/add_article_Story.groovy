description 'User can add article references'


scenario 'User can add article with correct fields', {
    given '', {
        post('/api/references/')
    }
    
    when '', {
        
    }
    
    then '', {
        
    }
}